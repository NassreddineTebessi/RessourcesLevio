import { FilterTableResourcePipe } from './filter-table-resource.pipe';

describe('FilterTableResourcePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTableResourcePipe();
    expect(pipe).toBeTruthy();

  });
});
